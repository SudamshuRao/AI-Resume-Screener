[pytest]
asyncio_mode = auto
pythonpath = services
testpaths =
    services/api/tests
    services/agents/tests
addopts =
    --cov=api
    --cov=agents
    --cov-report=term-missing
    --cov-fail-under=80